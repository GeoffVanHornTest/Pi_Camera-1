site_name: PI Camera
site_description: Raspberry Pi night vision motion camera with Gmail alerts
repo_url: https://github.com/GeoffVanHorn/PI_Camera

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [02-scripts]
